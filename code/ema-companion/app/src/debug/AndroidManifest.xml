<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Debug-only: permits cleartext HTTP to 10.0.2.2 (the emulator-to-host loopback)
         so the "Use local stub" Settings action can reach ema-api-stub. Release builds
         never merge this and keep the default cleartext-blocked behavior. -->
    <application android:networkSecurityConfig="@xml/network_security_config_debug" />

</manifest>
