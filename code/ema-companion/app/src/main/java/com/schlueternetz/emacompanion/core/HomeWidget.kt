package com.schlueternetz.emacompanion.core

/** Identifies a home-screen widget whose visibility the user can toggle in Settings. */
enum class HomeWidget {
    TODAY_PRODUCTION,
    PRODUCTION_SUMMARY,
    PRODUCTION_HISTORY,
}
