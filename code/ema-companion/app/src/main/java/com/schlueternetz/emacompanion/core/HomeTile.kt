package com.schlueternetz.emacompanion.core

/** Identifies a Home screen tile whose visibility the user can toggle in Settings. */
enum class HomeTile {
    TODAY_PRODUCTION,
    HISTORY_PRODUCTION,
    MODULE_HEALTH,
}
